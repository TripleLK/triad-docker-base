#+TITLE: Next Steps - Quantum Ridge
#+AUTHOR: Quantum Ridge
#+DATE: 2025-06-17
#+FILETAGS: :next-steps:quantum-ridge:batch-processing:cleanup:

* Immediate Priorities

** CRITICAL: Fix Option A Batch Processing Test
***HIGHEST PRIORITY*** - Previous batch job failed due to AWS minimum record requirement

*** Issue
- Batch job arn:aws:bedrock:us-east-1:891377295311:model-invocation-job/5jwtm04h53kp failed
- Error: "Batch job contains less records (5) than the required minimum of: 100"
- Need to rerun with all 107 records to properly test Option A metadata preservation

*** Solution Ready
Enhanced management commands now handle this automatically:
```bash
python manage.py submit_batch_test --use-all-records
```

This will:
1. Generate test data with all 107 AIJSONRecord entries
2. Include critical metadata fields (_processing_mode, _batch_info, etc.)
3. Submit to AWS Bedrock with proper batch size
4. Provide new job ID for monitoring

** Monitor New Batch Job
Once submitted, monitor with:
```bash
python manage.py check_batch_status --job-id=<new_job_id>
```

* Approach for Next Model

** File Review Strategy
1. **Start with Option A batch test** - This is the critical blocker
2. **Review management commands systematically**:
   - apps/content_extractor/management/commands/ contains ~15 commands
   - Some have suspicious dates (Thunder Ridge - June 2025)
   - Need to identify current vs obsolete implementations

** Key Files to Review
- bedrock_batch_workflow.py - Thunder Ridge (June 2025) - Check if legitimate
- batch_site_processor.py - Large file (43KB) - Review for current relevance
- All import/export commands - Consolidate duplicate functionality

** Cleanup Methodology
1. Check command creation dates and authors
2. Test if commands are functional and used
3. Look for duplicate functionality
4. Remove obsolete implementations
5. Update documentation

* Decisions Needed

** Management Command Consolidation
Multiple commands handle similar batch processing tasks:
- bedrock_batch_workflow.py
- submit_batch_inference.py  
- monitor_batch_jobs.py
- process_batch_results.py

Need to determine which is the current/preferred implementation.

** Test Infrastructure
Decide whether to:
- Keep new Django management commands for testing
- Integrate into main batch processing workflow
- Create separate test suite structure

* Handoff Context

** Current State
- content_extractor app cleaned of 400KB+ old files
- Test infrastructure properly organized as Django management commands
- Critical batch size issue identified and solution implemented

** What's Working
- AWS integration (aws_utils.py) 
- Enhanced prompt with metadata preservation
- Proper IAM role (BedrockBatchInferenceRole) identified and working

** What Needs Testing
- Full 107 record batch to verify Option A metadata preservation
- Whether Claude 3 Sonnet actually preserves critical metadata fields
- Import process with preserved metadata

** Resources Available
- All 107 AIJSONRecord entries in database
- Working AWS S3 integration
- Proper IAM permissions established
- Enhanced JSONL generation with metadata preservation prompt

* Risk Assessment

** Low Risk
- Django management command conversion - follows standard patterns
- File cleanup - removed clearly obsolete backup files

** Medium Risk  
- Management command review - need to avoid removing current functionality
- Batch processing consolidation - multiple implementations exist

** High Risk
- Option A testing depends on AWS batch processing working correctly
- Metadata preservation is critical for model association fix
- Import process changes if metadata structure changes

* Success Criteria

** Immediate (Next Session)
- [ ] Option A batch job submitted successfully with 107 records
- [ ] Job shows "Submitted" or "InProgress" status
- [ ] No AWS errors or permission issues

** Short Term
- [ ] Batch job completes successfully  
- [ ] Output files contain preserved metadata fields
- [ ] Import process works with preserved metadata
- [ ] EquipmentModel objects created successfully (not 0 like before)

** Long Term
- [ ] content_extractor app fully cleaned of obsolete files
- [ ] Consolidated batch processing workflow
- [ ] Documented and tested Option A implementation 