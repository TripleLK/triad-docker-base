#+TITLE: Next Steps - Tired Barstool → Holy Wheat Handoff
#+AUTHOR: Tired Barstool  
#+DATE: 2025-01-22
#+FILETAGS: :next-steps:handoff:holy-wheat:

* Immediate Priorities

** CRITICAL: Architecture Decision Required
The current sub_fields array approach has fundamental workflow problems. User testing revealed:
1. XPath visual selection is broken for sub-model sources
2. No creation workflow for nested sources exists
3. UI patterns don't match existing hierarchical menu system

**RECOMMENDATION**: Pivot to parent-child field relationships instead of sub_fields arrays.

** Priority 1: Fix XPath Selector Integration
- File: apps/xpath_content_extraction/extension/content/content-script.js
- Issue: saveXPath() method context handling between parent and sub-field modes
- Context switching logic needs complete overhaul
- Current implementation at lines 2189-2340 is non-functional

** Priority 2: Implement Source Creation Workflow  
- Missing UI for creating local_submodel/nonlocal_submodel sources
- Source type selection dropdown needs sub-model options
- Creation flow integration with existing patterns required

* Approach for Next Model

** Consider Architectural Pivot
User feedback strongly suggests moving from sub_fields arrays to:

```sql
ALTER TABLE xpath_content_extraction_fieldconfiguration 
ADD COLUMN parent_field_id INTEGER REFERENCES xpath_content_extraction_fieldconfiguration(id);
```

This would:
- Unify field management interfaces
- Enable natural hierarchical nesting  
- Leverage existing menu patterns
- Eliminate complex context switching

** If Continuing Current Approach
Focus on fixing these critical workflow issues:
1. XPath selector context modes (parent vs sub-field selection)
2. Source creation UI integration
3. Field type selection for nested structures

** Unified Interface Design
User wants sub-field menu to match main XPath Content Extraction interface:
- Multiple sources per field display
- Same source management patterns
- Add parent selector configuration section at top
- Add field creation popup with type selection (text/nested/list)
- This popup should work on both main and sub-field menus

* Key Files to Review

** Primary Implementation
- `apps/xpath_content_extraction/extension/content/content-script.js`: Main extension logic
- `apps/xpath_content_extraction/models.py`: Database models and validation
- `apps/xpath_content_extraction/views.py`: API endpoints

** Critical Methods Needing Attention
- `saveXPath()`: Broken context handling (lines 2189-2340)
- `populateSourceEditor()`: Missing sub-model creation options (lines 594-730)
- `initializeSubModelInterface()`: Context switching issues (lines 728-920)

** Database Considerations
- Migration 0003 added sub_fields JSONField
- Consider rollback if pivoting to parent_field_id approach
- Current validation logic supports sub-model types

* Decisions Requiring User Input

** Architecture Choice
1. **Fix current sub_fields approach**: Requires extensive XPath selector rework
2. **Pivot to parent_field_id**: Cleaner but requires schema changes and API rework

** Field Management Strategy  
- Should sub-fields use completely separate interface or unified one?
- How should field type selection work for nested structures?
- Where should parent selector configuration live?

** Implementation Scope
- Focus on local sub-models only, or include non-local (linked pages)?
- How deep should nesting go (sub-sub-fields)?
- Should existing hierarchical menu patterns be preserved or redesigned?

* Handoff Context

** What's Working
✅ Backend API handles sub_fields data correctly
✅ Database schema supports current approach  
✅ Validation logic is comprehensive
✅ Relative XPath generation concepts are sound

** What's Broken
❌ XPath visual selection completely non-functional
❌ No creation workflow for sub-model sources
❌ UI patterns don't match user expectations

** Technical Foundation
The backend implementation is solid and demonstrates the concepts work. The problem is entirely in the frontend workflow and UI patterns. The user's architectural suggestions about unified field hierarchies are excellent and would solve most of the current problems.

** User Expectations
Based on feedback, the user wants:
- Unified interface patterns between main and sub-field management
- Natural field creation workflows with type selection
- Parent selector configuration integrated cleanly
- Consistent with existing hierarchical menu system

The current implementation took a "separate sub-field management" approach, but user testing shows this creates workflow confusion and doesn't leverage existing patterns effectively.

** Recommendation
Consider the architectural pivot seriously. The parent_field_id approach would:
- Eliminate most current problems
- Create more intuitive workflows  
- Better match existing codebase patterns
- Enable unlimited nesting levels naturally
- Reduce code complexity significantly

Ready for Holy Wheat to take over with clear direction on critical issues and architectural considerations! 