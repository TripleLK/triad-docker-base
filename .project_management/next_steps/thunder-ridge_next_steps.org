#+TITLE: Next Steps - Thunder Ridge Handoff
#+AUTHOR: Thunder Ridge
#+DATE: 2025-06-17
#+FILETAGS: :next-steps:handoff:batch-processing:model-combination:

* Immediate Priorities

** CRITICAL TASK: Fix Batch Processing Model Combination
The batch processing workflow is fundamentally broken for model combination. The current issue is that AWS Bedrock batch processing loses the relationship metadata between overall details and model subset files.

** First Steps for Next Model
1. **Review Problem Analysis** in `thunder-ridge/2025-06-17_session_log.org`
2. **Choose Implementation Strategy** from options below
3. **Test with Existing Data** - we have good test data in `batch_output/`
4. **Verify Complete Workflow** - ensure models are properly created

* Implementation Strategy Options

** Option A: AI Prompt Modification (Recommended)
**Pros**: Preserves existing architecture, most comprehensive solution
**Cons**: Requires AI prompt testing and validation

### Steps:
1. **Modify AI Prompt** (`.project_management/ai_prompts/ai_json_to_lab_equipment_api.md`)
   - Add requirement to include `processing_mode` in output
   - Add requirement to include `batch_info` and `target_model_names` when applicable
   - Test prompt with sample inputs to ensure metadata preservation

2. **Update Batch Input Reconstruction**
   - Ensure `_processing_mode` and `_batch_info` are included in batch input JSONs
   - Verify AWS Bedrock preserves this metadata in outputs

3. **Test Full Pipeline**
   - Generate new batch export with metadata preservation
   - Run through AWS Bedrock batch processing  
   - Verify converted files contain processing mode information
   - Test import command with corrected files

** Option B: Smart File Analysis (Alternative)
**Pros**: Works with existing batch output, no AI prompt changes needed
**Cons**: More complex logic, may be less reliable

### Steps:
1. **Enhance Import Command Detection Logic**
   - Analyze JSON content to identify file types (presence of `models_data` vs general equipment data)
   - Use filename patterns and timestamps to group related files
   - Implement heuristic-based file relationship detection

2. **Create File Grouping Algorithm**
   - Group files by timestamp and domain matching patterns
   - Identify overall details files (contain comprehensive data, no models_data)
   - Identify model files (contain models_data arrays)

3. **Test with Current Data**
   - Apply new logic to existing `batch_output/json_files/`
   - Verify proper file grouping and model combination

** Option C: Hybrid Approach (Most Robust)
**Pros**: Fallback capabilities, works with both old and new data
**Cons**: Most complex implementation

### Steps:
1. **Implement Option A** for future batch processing
2. **Implement Option B** as fallback for existing data
3. **Auto-detect** which method to use based on file metadata presence

* Technical Implementation Details

** Key Files to Modify

### For Option A:
- `.project_management/ai_prompts/ai_json_to_lab_equipment_api.md` - Add metadata preservation requirements
- `apps/content_extractor/batch_processing.py` - Ensure metadata in batch inputs  
- Test with sample prompts to verify AI preserves `_processing_mode`

### For Option B:
- `apps/content_extractor/management/commands/import_ai_json_to_equipment.py`
  - Enhance `discover_json_file_groups()` method
  - Add content-based file type detection
  - Improve grouping logic for metadata-less files

### For Option C:
- Both above + auto-detection logic

** Data Analysis Insights
From investigating existing files:

### Two-Mode Export Structure (ai_json_exports/two_mode/):
```json
{
  "_processing_mode": "OVERALL_DETAILS",  // or "MODEL_SUBSET"
  "_batch_info": {
    "batch_number": 1,
    "total_batches": 3,
    "models_in_batch": ["model1", "model2"],
    "total_models": 9
  },
  "target_model_names": ["model1", "model2"],
  // ... rest of data
}
```

### Batch Processing Output Structure (batch_output/json_files/):
```json
{
  // NO _processing_mode
  // NO _batch_info  
  // NO target_model_names
  // Just the AI-generated content
}
```

## Root Cause Analysis
1. **Export Stage**: ✅ Correctly creates two-mode files with metadata
2. **Batch Input Stage**: ❓ May lose metadata when creating batch input
3. **AI Processing Stage**: ❌ AI doesn't preserve input metadata in output
4. **Conversion Stage**: ❌ Simple JSONL→JSON conversion loses relationship context
5. **Import Stage**: ❌ Cannot identify file types without metadata

* Testing Strategy

** Validation Steps
1. **Test Option A**: Create sample input with `_processing_mode`, run through batch processing, verify preservation
2. **Test Option B**: Run enhanced import command on existing `batch_output/json_files/`  
3. **Verify Model Creation**: Ensure models are actually created in database with proper relationships
4. **End-to-End Test**: Complete workflow from export → batch → import → verify pages

** Success Criteria
- **Models Created**: LabEquipmentPage has associated EquipmentModel objects
- **Specifications**: Models have proper EquipmentModelSpecGroup objects  
- **Relationships**: Clear linkage between overall equipment and specific models
- **Data Integrity**: No loss of model-specific data

* Current State Assessment

** Working Components
- ✅ AWS Bedrock batch processing infrastructure
- ✅ S3 upload/download functionality  
- ✅ Basic LabEquipmentPage creation
- ✅ Import command file discovery logic
- ✅ API serializer for page creation

** Broken Components  
- ❌ Model batch file identification
- ❌ File relationship preservation through batch processing
- ❌ Model data combination logic
- ❌ EquipmentModel creation from combined data

** Available Test Data
- `batch_output/json_files/` - 104 converted batch processing outputs
- `ai_json_exports/two_mode/` - Properly structured two-mode files for comparison
- `batch_output/batch_bedrock.jsonl.out` - Raw batch processing output

* Handoff Context

## Current Database State
- 15 LabEquipmentPage objects created successfully
- 0 EquipmentModel objects (the core problem)
- Basic pages have titles, descriptions, specifications but no models

## Critical Decision Point
Next model must choose implementation strategy based on:
- **Urgency**: Option B for immediate fix with existing data
- **Long-term**: Option A for robust future processing  
- **Safety**: Option C for maximum compatibility

## Files Ready for Testing
- Working import command at `apps/content_extractor/management/commands/import_ai_json_to_equipment.py`
- Test data directory `batch_output/json_files/` with 104 files
- Comparison data in `ai_json_exports/two_mode/` showing proper structure

The next model should start with Option B to get immediate results, then consider Option A for long-term robustness. 